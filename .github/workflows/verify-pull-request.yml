name: Verify pull request

on:
  pull_request:
    branches:
    - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: peaceiris/actions-hugo@v3
      with:
        hugo-version: '0.31.1'
    - run: hugo
